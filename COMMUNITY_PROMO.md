# 飞书多维表格审批提审台：产品化评审、Demo 方案与销售宣传稿

## 产品定位

### 建议对外产品名

**飞书多维表格审批提审台**

副标题：**批量预检、材料归集、审批发起、状态回填的一体化插件**

不要把产品包装成“某客户的付款插件”。付款是第一个模板，真正要卖的是一套“多维表格记录 → 飞书审批”的通用连接层。

### 你卖的不是 Demo，而是三层产品

1. **通用产品底座**：嵌入多维表格的提审台、批次预检、附件归集、审批发起、状态同步。
2. **场景模板**：付款、报销、采购、合同、项目结项等预置模板。
3. **定制与支持**：字段映射、审批控件映射、权限配置、部署上线、培训和持续维护。

建议采用“产品费 + 一次性实施定制费 + 年度支持费”的商业结构。群里先卖场景和 Demo，不要一上来公布复杂报价；把 CTA 设为“预约演示/评估一个流程”。

## 应该展示什么产品？

### 结论：展示“可运行的通用版 Demo”，付款场景作为第一个模板

不要展示源代码，也不要只放一张静态 UI 截图。最有效的是一个脱敏的 Demo Base + 插件实例，能在 3 分钟内走完一次完整闭环：

`勾选多条记录` → `预检发现问题` → `补充附件` → `生成材料并发起审批` → `审批链接/状态回填`

### Demo 必须包含的 6 个画面

1. **多维表格数据层**：展示勾选框、业务明细、附件、状态字段。
2. **预检通过状态**：记录数、金额、审批定义、附件数量一眼可见。
3. **预检失败状态**：故意放一条“未验收/缺附件”的记录，展示可读错误，而不是让流程直接报错。
4. **批量材料生成**：展示截图、CSV、凭证被自动归集。
5. **审批发起结果**：展示审批实例 Code、审批链接、流程编号。
6. **回填后的 Base**：展示付款进度/审批状态/时间字段已自动更新。

### 最适合群里的演示形式

- **20～30 秒录屏/GIF**：只展示上述闭环，不讲架构。
- **一张流程图**：放在录屏下面，帮助用户理解产品边界。
- **一个可预约的 Demo**：对有兴趣的人再展示后台配置和定制方式。

群里展示的是“产品如何解决问题”，销售演示才展示“如何适配不同 Base 和审批定义”。

### 当前代码里的 Demo 状态

项目已经有 `?demo=1` 的前端演示入口和脱敏预览数据，可以作为销售 Demo 的起点；但当前演示数据仍带有客户付款语境，且提交按钮没有完整的前端模拟结果。对外展示前建议：

- 换成通用的“供应商付款/费用报销”虚构数据；
- 增加 Demo 模式下的假审批返回（批次号、实例 Code、审批链接、回填结果）；
- 增加一条可切换的校验失败记录；
- 明确 Demo 不会访问真实 Base、不会发起真实审批。

## 现有能力（已在客户项目中跑通的能力形态）

- 在当前 Base 中读取已勾选的待提审记录，并展示批次数量、金额、审批定义和逐条校验结果。
- 提审前自动检查付款进度、验收、合同/订单、发票/凭证、实际成本、项目/资源关联、收款账户等条件。
- 根据付款形式路由到不同审批定义；不同付款形式或不同收款账户不能混在同一批次。
- 自动生成付款明细截图和 CSV，归集多维表格附件，转换为飞书审批可识别的附件编码。
- 对需要临时补充的材料（例如收款二维码、补充凭证）提供插件内上传，不强迫 Base 增加临时字段。
- 以当前操作用户发起飞书原生审批，回填审批实例 Code、流程编号、审批链接和付款进度。
- 支持手动刷新/同步审批状态，并将审批中、通过、驳回等状态回填到付款执行明细。
- 通过飞书 OAuth 获取用户身份，适合以妙搭应用方式交付，不依赖用户电脑上的 CLI 或脚本。

## 适合抽象成通用能力的部分

### 1. “选中记录 + 批次预检”引擎

把字段名、允许值、批次分组规则和校验规则从业务代码中抽出，变成配置或规则表。任何“勾选多行后提交审批”的场景都可以复用。

### 2. “Base 附件 → 审批附件”适配器

统一处理附件 token 解析、去重、下载、大小限制、上传和 file code 清单回填。这个模块不应绑定付款场景，可用于合同、报销、采购、项目结项等审批。

### 3. 审批定义自检与动态必填项

提交前读取审批定义，校验审批 Code、完整名称、控件 ID、必填项和发起人选审批人配置。审批表单变更时给出可读错误，避免静默失败。

### 4. 审批实例回填与状态同步

以批次号/幂等 UUID 关联多条 Base 记录，回填实例 Code、流水号、审批链接、状态和时间。后续可把主动同步扩展为定时任务或事件驱动。

### 5. 嵌入式插件交互层

复用当前 Base 上下文、当前用户 OAuth、预检摘要、补充附件上传、进度反馈和结果弹窗，形成一个“表格内操作台”模板。

## 当前客户定制项（不能直接当成通用能力宣传）

- 固定的中文字段名、Base Token、表 ID、同步表 ID 和审批链接。
- 三种付款形式：对公支付、小荷包支付、云账户支付。
- 云账户审批金额按实际成本增加 6.65% 服务费。
- 对公付款的开户省/市、账户类型等账户字段解析与校验。
- 小荷包收款二维码的特殊必填逻辑。
- 资源入库、立项、结项、付款执行明细之间的客户专属关联字段。
- 目前配置默认仍指向带“【测试】”前缀的审批定义，正式交付前必须替换为生产配置并做一轮真实权限/表单回归。

## 通用化产品建议

### P0：先做“配置化适配层”

- `BaseAdapter`：表、字段、视图、勾选条件、回填字段。
- `ApprovalAdapter`：审批 Code、名称、控件映射、金额/日期/附件/联系人等字段转换。
- `ValidationRules`：单行规则、批次规则、分组键、允许带问题提交策略。
- `AttachmentPolicy`：来源字段、是否去重、上传类型、大小上限、是否允许插件临时上传。

### P1：补齐交付与运维能力

- Test/Prod 环境切换和配置版本号。
- 提交前展示“将写入哪些字段、将发起哪个审批、金额是多少”的确认页。
- 上传/提审/回填失败的重试、幂等保护和操作日志。
- 同步任务的定时执行、失败告警和最近同步时间。
- 管理员配置页：字段映射、审批定义、控件 ID、权限范围。

### P2：扩大通用场景

- 项目结项：勾选记录后生成明细并一键提起结项审批。
- 报销/采购/合同：复用预检、附件归集、审批发起、状态回填。
- 将“付款形式路由”升级为任意业务类型/审批定义路由。

## 建议的商业套餐

### 标准版

- 一个通用提审台实例；
- 一个 Base/一类审批流程模板；
- 标准字段映射和审批状态回填；
- 基础部署文档和一次使用培训。

### 定制版

- 按客户现有 Base 字段和审批定义做适配；
- 多审批路由、复杂批次规则、特殊附件控件；
- 妙搭部署、OAuth/权限配置、联调和上线陪跑；
- 按项目范围报价，不承诺“所有审批开箱即用”。

### 持续支持

- 审批表单变更后的控件映射调整；
- 飞书接口或权限变化后的兼容修复；
- 运行问题排查、日志和同步任务维护；
- 可按月/按年收取支持费。

这三层要在 Demo 后再讲。用户先看到“流程真的跑通”，再讨论“你们的流程需要哪些适配”。

## 群内宣传稿（产品销售版，可直接发）

很多团队已经把业务明细放进飞书多维表格，但真正提审时还要人工筛选、复制字段、下载附件、重新整理材料，审批完成后又要手工回填状态。

我们做了一套可嵌入飞书多维表格的「审批提审台」：

勾选待付款明细 → 插件自动核对验收/合同/发票/账户 → 生成截图和 CSV → 归集并上传凭证 → 发起飞书原生审批 → 审批编号、链接和状态自动回填 Base。

付款只是其中一个模板，报销、采购、合同、项目结项等“多行记录批量提审”的流程也可以复用。

我们提供：

- 通用提审台产品；
- 按你们的 Base 字段和审批定义做适配；
- 部署、权限配置、上线培训和持续支持。

如果你们有一个“多维表格里已经有数据，但提审仍靠人工整理”的流程，欢迎回复 **「提审台」**，可以先看 3 分钟 Demo，再评估是否适合定制。

## 群内宣传稿（稍长版，适合配 Demo 录屏）

**多维表格里的审批，能不能少做几次复制粘贴？**

我们把这类流程产品化成了一个嵌入飞书多维表格的「审批提审台」：

1. 在【付款执行明细】里勾选多条记录；
2. 插件实时校验付款条件和审批必填项；
3. 自动生成明细截图、CSV，并归集 Base 附件；
4. 在插件内补充二维码/其他审批附件；
5. 以当前用户发起原生飞书审批；
6. 审批实例 Code、流程编号、审批链接和状态自动回填。

最有价值的不是“自动点了提交”，而是把提审前最容易出错的准备工作变成可见、可检查、可追溯的批处理流程。

目前已经用付款场景验证了完整闭环。产品底座可以复用，具体项目只需要配置 Base 字段、审批 Code、控件映射和校验规则。

我们提供标准产品，也支持按企业现有多维表格和审批流程做定制交付。

想看 Demo 或评估一个现有流程，欢迎回复 **「提审台」**。

## Demo 讲解脚本（3 分钟）

### 0:00–0:30：先讲痛点

“这类流程的问题通常不在审批本身，而在审批前后的人工整理：筛选记录、核对条件、收集附件、复制字段、回填状态。”

### 0:30–1:30：跑通主流程

展示 Demo Base 中勾选 3 条记录，打开插件，说明插件自动识别当前 Base、汇总批次金额、路由审批定义，并展示校验结果。

### 1:30–2:00：展示异常处理

切换到一条缺附件或未验收记录，展示可读的阻断原因；补充附件后重新预检，说明“错误前置”比提交后报错更重要。

### 2:00–2:40：展示自动化结果

点击提交，展示截图/CSV/附件自动上传，审批实例 Code 和链接生成，并回到 Base 展示状态回填。

### 2:40–3:00：转入销售

“付款是当前模板，底座可以复用到报销、采购、合同和项目结项。你们如果已经有一张承载业务明细的 Base，可以带一个流程来，我们按现有字段和审批定义评估定制工作量。”

## 配图/演示建议

建议用一张“三段式”图或 20～30 秒录屏，不要用客户真实数据：

`多维表格勾选记录` → `插件预检 + 附件归集` → `审批发起 + 状态回填`

画面上优先展示：

- 左侧：选中记录、金额、附件数量、校验通过/失败；
- 右侧：付款事由、期望日期、补充附件和“生成材料并发起审批”；
- 结果页：实例 Code、审批链接、状态已回填。

### Demo 数据准备

- 使用虚构项目、虚构金额和虚构附件；
- 准备一组正常记录、一条校验失败记录；
- 至少演示一次附件上传和一次状态回填；
- 审批定义使用“演示版/测试版”，不要在公开群里暴露真实审批 Code、Base Token 或客户链接；
- 录屏里隐藏 OAuth 回调地址、应用 ID、文件 token 和内部字段 ID。

## 对外口径

- 可以说：已在客户付款场景验证，可抽象为通用的批量提审能力。
- 可以说：提供标准产品，并支持按客户 Base 和审批流程做定制交付。
- 不要说：开箱即用、适配所有审批、完全无人值守付款。
- 需要说明：正式落地仍需配置目标 Base 字段、审批 Code/控件映射、OAuth 权限和附件权限；实际打款仍由财务执行。
